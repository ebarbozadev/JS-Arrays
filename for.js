const numeros = [100, 200, 300, 400, 500, 600];

for (let i = 0; i < numeros.length; i++){
// Criamos um laço com o comando for
// Criamos uma variável de controle e atribuimos o valor 0
// .length é um comando que "anda" pela variável
// ++ é um comando que adiciona + 1 ao valor anterior da variável
    console.log(i, numeros[i]);
}